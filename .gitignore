node_modules/
*.log
.DS_Store
# generated game output is kept; build/export artifacts are not
games/**/exports/
*.c3p

# local-only reference project (kept out of the repo to limit size)
reference/pop/
# purchased source (license forbids publishing) — never commit
reference/nuke-them-all/

# DALA incorporates licensed RTS code/art — local only, never publish
games/dala/
games/dala-play/
